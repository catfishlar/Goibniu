<%= form_for(@db_status) do |f| %>
  <% if @db_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@db_status.errors.count, "error") %> prohibited this db_status from being saved:</h2>

      <ul>
      <% @db_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :database_id %><br>
    <%= f.number_field :database_id %>
  </div>
  <div class="field">
    <%= f.label :request_date %><br>
    <%= f.date_select :request_date %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
